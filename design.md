🎫 TICKET CAL-01 (Updated)

Components to build:

InputField component — reusable, receives props
LoginForm component — uses InputField twice
App — renders LoginForm

What props does your InputField need?
That's for you to figure out. Think about what changes between email and password field and what stays same.

Your task right now:

Set up your React app with Vite
Build this component structure
Make the form controlled
Console.log on submit